// Special Characters in regular expression(MetaCharacters)
// (.) to replace a character, (*) to replace multiple characters and watch out for the first and last characters (^) this works upon the whole string identifying the first character in the string
// ($) It checks for the end part of the characters in a string (?) works as an ooptional Character in any missing word

// let regex = /Welc?me/
// str1 = "Welcome to my channel, have a greate day welcome"
// let output =regx.test(str1);
// console.log(output);
// let op = regex.exec(str1);
// console.log(op);

